/// Dart has assertions. They are disabled by default.
/// You can turn them on by running or compiled the script in checked mode.
/// Assert is a special statement, not a method.

void main() {
  assert("If you see this, then you have assertions enabled" == null);
}
